[project]
name = "wiki-racer"
version = "0.1.0"
description = "Outputs the shortest path between two given Wikipedia pages."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "lxml>=6.0.2",
]
